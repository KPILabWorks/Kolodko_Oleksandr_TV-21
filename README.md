## Колодько Олександр ТВ-21
## 12 варіант
## Практичні завдання

Цей репозиторій містить два приклади оптимізації роботи з даними: обчислення добутку елементів списку за допомогою `functools.reduce` та швидке обчислення статистики в `DataFrame` за допомогою `.apply()`. Також додано аналіз часових рядів із використанням `resample()`, та прогноз енергоспоживання.

## 📌 Оберіть тему:
1. [Обчислення добутку елементів списку](#обчислення-добутку-елементів-списку)
2. [Швидке обчислення статистики в DataFrame](#швидке-обчислення-статистики-в-dataframe)
3. [Агрегація часових рядів та порівняння точності прогнозів](#агрегація-часових-рядів-та-порівняння-точності-прогнозів)
4. [Прогнозування енергоспоживання з використанням ARIMA, Prophet та експоненційного згладжування](#прогнозування-енергоспоживання-з-використанням-arima-prophet-та-експоненційного-згладжування)
5. [Виявлення аномалій за допомогою Isolation Forest](#виявлення-аномалій-за-допомогою-isolation-forest)

---

## Обчислення добутку елементів списку

Цей код використовує модуль `functools` та його функцію `reduce()` для обчислення добутку всіх елементів у списку чисел.

### Опис коду
1. Імпортується функція `reduce` з модуля `functools`.
2. Визначається функція `multiply(x, y)`, яка повертає добуток двох чисел.
3. Створюється список чисел `numbers = [2, 3, 5, 7, 11]`.
4. Використовується `reduce(multiply, numbers)`, що застосовує `multiply()` послідовно до всіх елементів списку, обчислюючи загальний добуток.
5. Виводиться результат у консоль.

### Приклад виводу
```
Добуток елементів списку: 2310
```

### Вимоги
Python 3.x

### Як запустити
```bash
python script.py
```

---

## Швидке обчислення статистики в DataFrame

Цей проєкт демонструє, як використовувати `.apply()` для обчислення кастомної метрики для кожного рядка у великому `DataFrame`.

### Опис коду
1. **Створення великого DataFrame**:
   - Генерується 1 000 000 рядків із випадковими значеннями для довжини тексту, кількості слів і тональності.

2. **Обчислення кастомної метрики**:
   - Використовується `.apply()` для розрахунку спеціальної метрики: 
     ```
     (середня довжина слова) * (тональність тексту)
     ```
   - Це дозволяє швидко аналізувати текстові дані на основі їхніх характеристик.

3. **Вивід результату**:
   - Відображаються перші 5 рядків з розрахованою метрикою.

### Вимоги
- Python 3.x
- Pandas
- NumPy

### Як запустити
```bash
pip install pandas numpy
python script.py
```

---

## Агрегація часових рядів та порівняння точності прогнозів

У цьому розділі продемонстровано використання `resample()` для перетворення щоденних даних про споживання енергії на тижневі середні значення, а також порівняння точності прогнозів для агрегованих та оригінальних даних.

### Опис коду
1. **Генерація випадкових щоденних даних про споживання енергії**.
2. **Перетворення щоденних даних у тижневі середні значення**:
   - Використовується `df.resample('W').mean()` для створення агрегованого ряду.
3. **Порівняння точності прогнозів**:
   - Виконується прогнозування на основі оригінального та агрегованого рядів.
   - Аналізується точність прогнозу.

### Вимоги
- Python 3.x
- Pandas
- NumPy
- Matplotlib (для візуалізації)

### Як запустити
```bash
pip install pandas numpy matplotlib
python script.py
```

---

## Прогнозування енергоспоживання з використанням ARIMA, Prophet та експоненційного згладжування

Цей розділ демонструє, як використовувати різні методи прогнозування часових рядів для моделювання та прогнозування енергоспоживання. Порівнюються три методи: експоненційне згладжування (Holt-Winters), ARIMA та Facebook Prophet.

### Опис коду

1.  **Завантаження даних:**
    *   Код зчитує дані про енергоспоживання з файлу `energy_data.csv`.  Очікується, що файл містить колонки `timestamp` (з датою та часом) та `energy_consumption` (з числовим значенням споживання).
2.  **Розділення даних на навчальну та тестову вибірки:**
    *   Дані розділяються на навчальну вибірку (80%) для навчання моделей та тестову вибірку (20%) для оцінки їхньої точності.
3.  **Методи прогнозування:**
    *   **Експоненційне згладжування (Holt-Winters):** Використовується модель `ExponentialSmoothing` з бібліотеки `statsmodels` для прогнозування на основі тренду та сезонності даних.  Потребує налаштування параметру `seasonal_periods`, який відповідає періоду сезонності.
    *   **ARIMA:**  Використовується модель `ARIMA` з бібліотеки `statsmodels` для прогнозування на основі автокореляції даних. Потребує налаштування порядку (p, d, q), що визначає авторегресійну (AR), інтегровану (I) та ковзну середню (MA) частини моделі.
    *   **Facebook Prophet:** Використовується бібліотека `prophet`, розроблена Facebook, для прогнозування часових рядів, що мають виражену сезонність та тренди.
4.  **Оцінка моделей:**
    *   Для кожної моделі обчислюються дві метрики:
        *   **RMSE (Root Mean Squared Error):** Середньоквадратичне відхилення прогнозованих значень від фактичних. Чим менше, тим краща модель.
        *   **MAE (Mean Absolute Error):** Середня абсолютна помилка. Чим менше, тим краща модель.
5.  **Візуалізація:**
    *   Графічно відображаються фактичні значення та прогнози кожної моделі для тестової вибірки, що дозволяє візуально оцінити їхню точність.

### Вимоги

*   Python 3.x
*   pandas
*   statsmodels
*   scikit-learn
*   prophet
*   matplotlib

### Як запустити

1.  **Встановіть необхідні бібліотеки:**

    ```bash
    pip install pandas statsmodels scikit-learn prophet matplotlib
    ```

2.  **Підготуйте дані:**
    *   Створіть файл `energy_data.csv` з даними про енергоспоживання. Переконайтеся, що файл містить колонки `timestamp` (у форматі дати/часу) та `energy_consumption` (числові значення). Приклад структури файлу:

        ```csv
        timestamp,energy_consumption
        2023-01-01 00:00:00,150
        2023-01-01 01:00:00,155
        2023-01-01 02:00:00,148
        ...
        ```

    *   Поради щодо отримання даних:
        *   Шукайте загальнодоступні набори даних на Kaggle або урядових порталах.
        *   Згенеруйте синтетичні дані за допомогою наданого Python скрипту (див. попередні інструкції).
3.  **Запустіть скрипт Python:**

    ```bash
    python main.py
    ```

### Налаштування

*   **Частота даних:** Обов'язково вкажіть правильну частоту даних (наприклад, 'H' для погодинних, 'D' для щоденних) у коді. Змініть `data = data.asfreq('H')` на відповідне значення.
*   **Параметри моделей:**
    *   `seasonal_periods` у `exponential_smoothing`: Налаштуйте відповідно до періоду сезонності у ваших даних.
    *   `order` у `arima_forecast`: Підберіть оптимальний порядок (p, d, q) для моделі ARIMA.
*   **Шлях до файлу з даними:** Переконайтеся, що шлях до файлу `energy_data.csv` вказано правильно.

### Інтерпретація результатів

Порівняйте значення RMSE та MAE для кожної моделі, щоб визначити, яка з них найкраще підходить для ваших даних. Графік візуалізації також допоможе візуально оцінити точність прогнозів. Врахуйте, що правильний підбір параметрів та врахування особливостей даних (сезонність, тренди, викиди) є критичними для досягнення хороших результатів прогнозування.

## Виявлення аномалій за допомогою Isolation Forest

Цей код реалізує алгоритм Isolation Forest для виявлення аномалій у двовимірних даних.

### Опис коду
1. **Генерація даних:**
   - Створюється набір випадкових точок, що розподілені нормально.
   - Додаються кілька аномальних точок, які значно відрізняються від основного розподілу.
2. **Застосування Isolation Forest:**
   - Використовується `IsolationForest` із `sklearn.ensemble` для ідентифікації аномалій.
   - Визначається коефіцієнт `contamination`, який вказує очікувану частку аномальних точок у наборі даних.
3. **Візуалізація результатів:**
   - Будується розсіювання точок, де аномалії позначені іншим кольором.
   - Відображається шкала кольорів для ідентифікації аномальних точок.

### Вимоги
- Python 3.x
- NumPy
- Pandas
- Scikit-learn
- Matplotlib

### Як запустити
```bash
pip install numpy pandas scikit-learn matplotlib
python isolation_forest.py
```
